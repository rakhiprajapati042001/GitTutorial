my first vertion
now i eant some new changes in this file

 all about traking the files
---git status 
---git init
git add --a
----git commit -m"msg"
----git commit
git log --- too see our all commit
git status....

git diff--- to compare the staging aarea fila and working directory file

git rm first.txt ----------------direct remove file from cmd

git rm --cached filename --------------> to untracked the file from git

git checkout --file-name ------------>to get back on privieus commit after the change we need to previous commit agtre the changes

(use "git rm --cached <file>..." to unstage)

git checkout -f ----> for all restore commit previous
